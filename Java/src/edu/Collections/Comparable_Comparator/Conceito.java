package edu.Collections.Comparable_Comparator;

public class Conceito {
    /*
     * Comparable: Fornece uma única sequência de ordenação, usando o compareTo(). Podemos ordenar a coleção com base em um único elemnto, como id, nome e preço.
     *          . Afeta a classe original, a classe atual é alterada
     *          . Oferece o método comparteTo para odernar os elementos.
     *          . Presente no pacote Java.lang
     * 
     *
     */



     
    /*
     * Comparator: Fornece o método compare() para ordenar elemntos. Fornce múltiplas sequências de ordenação. Podemos ordenar a coleção com base em múltiplos elementos, como id, nome, preço, etc.
     *          . Não afeta a classe original
     *          . Presente no pacote Java.util 
     */


    //  Para comparar inteiros, você deve usar Integer.compare(int x, int y), que é um método estático que aceita dois inteiros e retorna um inteiro:

    //  Um valor negativo se x for menor que y.
    //  Zero se x for igual a y.
    //  Um valor positivo se x for maior que y.
}
